{
    "scripts": {
        "start": "pwd && ls && cd node_modules && cd .bin && pwd && bash netlify-lambda.cmd && serve src && tsc",
        "build": "./node_modules/.bin/netlify-lambda && build src & tsc"
    },
    "dependencies": {
        "@netlify/functions": "^1.2.0",
        "express": "^4.18.1",
        "netlify-lambda": "^2.0.15",
        "pg": "^8.8.0",
        "pg-native": "^3.0.1",
        "serverless-http": "^3.0.2"
    },
    "devDependencies": {
        "@types/pg": "^8.6.5",
        "typescript": "^4.8.3"
    }
}
